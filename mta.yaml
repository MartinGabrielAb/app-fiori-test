_schema-version: "3.2"
ID: appfioritest
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: appfioritest-destination-content
  type: com.sap.application.content
  requires:
  - name: appfioritest-destination-service
    parameters:
      content-target: true
  - name: appfioritest-repo-host
    parameters:
      service-key:
        name: appfioritest-repo-host-key
  - name: appfioritest-uaa
    parameters:
      service-key:
        name: appfioritest-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: appfioritest_html_repo_host
          ServiceInstanceName: appfioritest-html5-service
          ServiceKeyName: appfioritest-repo-host-key
          sap.cloud.service: appfioritest
        - Authentication: OAuth2UserTokenExchange
          Name: appfioritest_uaa
          ServiceInstanceName: appfioritest-xsuaa-service
          ServiceKeyName: appfioritest-uaa-key
          sap.cloud.service: appfioritest
        existing_destinations_policy: update
  build-parameters:
    no-source: true
- name: appfioritest-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: appfioritest-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - appfioritest.zip
      name: appfioritest
      target-path: resources/
- name: appfioritest
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: appfioritest-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: appfioritest-destination-service
    service-plan: lite
- name: appfioritest-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: appfioritest-xsuaa-service
    service-plan: application
- name: appfioritest-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: appfioritest-html5-service
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
